<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wang.spingboot06mybatis.mapper.EmployeeMapper">
    <select id="getEmployeeById" parameterType="java.lang.Integer" resultType="com.wang.spingboot06mybatis.entity.Employee">
        select * from employee where id=#{id}
    </select>
</mapper>